mutation (
    $input: CreateCustomerInput!
) {
    createCustomer(
        input: $input
    ) {
        customer {
            id
            identifier
            firstName
            lastName
            created
        }
    }
}
